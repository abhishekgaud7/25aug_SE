#include<stdio.h>
main()
{
	int length,bredth;
	printf("Enter the length:");
	scanf("%d",&length);
	printf("\nEnter the bredth:");
	scanf("%d",&bredth);
	printf("\nArea of rectangle %d",length*bredth);
}
